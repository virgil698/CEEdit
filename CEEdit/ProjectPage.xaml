<Page x:Class="CEEdit.ProjectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProjectPage">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 上部菜单 -->
        <StackPanel Orientation="Horizontal" Margin="20">
            <!-- 搜索区域 -->
            <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4" Background="White">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- 搜索按钮 -->
                    <Button Width="39" Height="39" Background="White" BorderThickness="0" Cursor="Hand">
                        <Image Source="pack://application:,,,/resources/search.ico" Width="25" Height="25"/>
                    </Button>
                    <!-- 搜索框 -->
                    <TextBox x:Name="SearchTextBox" Width="161" Height="39" BorderThickness="0"
                             Background="Transparent" Padding="0" FontSize="14" VerticalContentAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="#FF999999"/>
                                <Setter Property="Text" Value="搜索项目..."/>
                                <Style.Triggers>
                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter Property="Text" Value=""/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </StackPanel>
            </Border>

            <Button Content="创建项目" Width="120" Height="39" Margin="10,0" Background="#FF4A90E2"
                    Foreground="White" BorderThickness="0" FontSize="14" FontWeight="SemiBold"
                    Click="CreateProjectButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3A77C3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2C60A3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="打开" Width="80" Height="39" Background="#FF4A90E2" Foreground="White"
                    BorderThickness="0" FontSize="14" FontWeight="SemiBold" Click="OpenProjectButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3A77C3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2C60A3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- 下部项目列表 -->
        <ListBox Grid.Row="1" Margin="20" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="ItemBorder" Background="White" BorderBrush="#FFE0E0E0"
                            BorderThickness="0,0,0,1" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="resources/logo.ico" Width="40" Height="40" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="项目名称" FontWeight="SemiBold" Foreground="#FF333333" FontSize="14"/>
                                <TextBlock Text="项目路径" Foreground="#FF999999" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <DataTemplate.Triggers>
                        <Trigger Property="ListBoxItem.IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#FFEAF6FF"/>
                        </Trigger>
                        <Trigger Property="ListBoxItem.IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#FFF0F7FF"/>
                        </Trigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>