<Page x:Class="CEEdit.ConversionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ConversionPage">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 第一行：选择包文件夹 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,20,20,10">
            <TextBlock Text="{Binding LabelConfigFolder}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#FF525252" FontSize="14"/>
            <TextBox x:Name="PathTextBox" Width="350" Height="39" IsReadOnly="True" 
                     Background="#FFFFFFFF" BorderBrush="#FFE0E0E0" BorderThickness="1" 
                     Padding="10" FontSize="14" Tag="{Binding TextBoxPlaceholder}">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="#FF999999"/>
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                            <VisualBrush.Visual>
                                                <Label Content="{Binding RelativeSource={RelativeSource AncestorType=TextBox}, Path=Tag}" 
                                                       Foreground="#FF999999" FontSize="14"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <Button Content="{Binding ButtonSelectPath}" Width="100" Height="38" Margin="10,0,0,0"
                    Background="#FF4A90E2" Foreground="White" BorderThickness="0" 
                    FontSize="14" FontWeight="SemiBold" Click="SelectPathButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3A77C3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2C60A3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- 第二行：选择插件配置 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,10,20,20">
            <TextBlock Text="{Binding LabelPluginConfig}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#FF525252" FontSize="14"/>
            <RadioButton Content="{Binding PluginItemsAdder}" Margin="0,0,10,0" GroupName="PluginGroup"
                         Foreground="#FF525252" FontSize="14" Padding="10,0"/>
            <RadioButton Content="{Binding PluginNexo}" GroupName="PluginGroup"
                         Foreground="#FF525252" FontSize="14" Padding="10,0"/>
        </StackPanel>

        <!-- 右下角按钮 -->
        <Button Content="{Binding ButtonStartConversion}" Width="120" Height="36" HorizontalAlignment="Right" Grid.Row="2" Margin="0,0,25,-300"
                Background="#FF4A90E2" Foreground="White" BorderThickness="0" 
                FontSize="14" FontWeight="SemiBold">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF3A77C3"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF2C60A3"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Page>